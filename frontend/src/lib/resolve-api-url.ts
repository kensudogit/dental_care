/**
 * Go API base URL (no trailing slash).
 * Local: defaults to http://localhost:8080
 * Railway Web: set API_URL to the API service public HTTPS URL.
 */
export function resolveApiUrl(): string {
  const raw = process.env.API_URL?.trim()
  if (!raw) {
    return 'http://localhost:8080'
  }
  return raw.replace(/\/+$/, '')
}

export function isProductionDeploy(): boolean {
  return process.env.NODE_ENV === 'production'
}

export function graphQLConnectionHint(): string {
  if (isProductionDeploy()) {
    return 'Web ????????? API_URL ? API ??? URL?https://…?????????????????????API ?????????????????????'
  }
  return '?????????dental_care?? npm run dev ?????api ? web ????????????????????frontend ??? npm run dev ?? API ????????'
}
